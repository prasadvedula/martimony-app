# This file is kept for reference only.
# Frontend is deployed to Vercel (vercel.com).
# Backend is deployed to Railway — see backend/railway.toml.

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
